import React from 'react';
import ReactDOM from 'react-dom';
import Sidebar from 'sidebar';

const container = ReactDOM.createRoot(document.getElementById('popcorn-sidebar'));
container.render(<Sidebar />, container);
